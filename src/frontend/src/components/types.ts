
export interface CardProps {
    id: number
    title: string;
    body: string;
}

export interface Person {
    id: number
    fullname: string;
    gender: string;
    foto: string;
    note: string;
    create_date: string;
}